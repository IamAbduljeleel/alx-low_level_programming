#ifndef MACRO_H
#define MACRO_H

/**
 * File: 3-function_like_macro.h
 * @x: input value
 *
 * Desc: Header file that computes the absolute value
 */

#define ABS(x) ((x) < 0 ? (-1) * (x) : (x))

#endif
